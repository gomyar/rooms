
var game = {
    connecting: true
};


$( document ).ready(function() {

    // request token
    // connect_player

    // redirect to node
	// connect websocket
    // player_connects

	// load room

 	// start processing

    turtlegui.reload()
});
